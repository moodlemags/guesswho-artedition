gameChoice = prompt("Would you like to play Guess Who or Memory?");
  if (gameChoice === "Guess Who") {
    window.location.assign(index.html);
  }

//prompt of which game to pick
//array of artists, paintings and attributes
